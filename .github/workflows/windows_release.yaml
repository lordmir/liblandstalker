name: "build-release-windows"
on:
  push:
  release:
    types: [published]
  pull_request:
    types: [opened, edited, synchronize, reopened]
    branches: master
jobs:
  build:
    name: Windows release build
    runs-on: windows-2022
    env:
      BUILD_TYPE: Release
    strategy:
      fail-fast: false
    steps:
      - name: Setup cmake
        uses: jwlawson/actions-setup-cmake@v2
        with:
          cmake-version: '3.30.x'

      - name: Checkout sources
        uses: actions/checkout@v5
        with:
          path: liblandstalker
          submodules: 'recursive'

      - name: 'Configure'
        working-directory: ${{github.workspace}}/liblandstalker
        run: |
          cmake -S . -B build

      - name: 'Build'
        working-directory: ${{github.workspace}}/liblandstalker
        run: |
          cmake --build build --config Release --verbose

      - name: Compress artifacts
        working-directory: ${{github.workspace}}/liblandstalker/build/liblandstalker/Release
        run: Compress-Archive -Path liblandstalker.*, -Destination liblandstalker.zip

      - name: Archive artifacts
        uses: actions/upload-artifact@v4
        with:
          name: landstalker_binary_windows
          path: ${{github.workspace}}/liblandstalker/build/Release/liblandstalker.zip
